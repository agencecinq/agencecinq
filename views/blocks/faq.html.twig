{#
 # Blocks: FAQ
 # Accordion of questions. First item open by default. FAQPage microdata.
 #}

{{- include( 'blocks/_layout.html.twig', { paddings: block.layout.paddings, id: block.id } ) -}}

{%- set items = block.items | default([]) | filter(item => item.question and item.answer) -%}

<section
	id="{{ block.id }}"
	class="bg-page text-primary"
	{{ block.content.title ? 'aria-labelledby="' ~ block.id ~ '-title"' }}
	itemscope
	itemtype="https://schema.org/FAQPage"
>
	<div class="container space-y-14">
		{%- if block.content.overline or block.content.title -%}
			<header class="space-y-6">
				{{-
					include(
						'components/eyebrow.html.twig',
						{
							title: block.content.overline
						},
						with_context: false
					)
				-}}

				{%- if block.content.title -%}
					<{{ block.content.heading | default('h2') }} id="{{ block.id }}-title" class="text-title-xl max-w-180">
						{{- block.content.title -}}
					</{{ block.content.heading | default('h2') }}>
				{%- endif -%}
			</header>
		{%- endif -%}

		{%- if items | length -%}
			<cinq-accordion-group class="block max-w-190">
				{%- for item in items -%}
					{%- set question_id = block.id ~ '-question-' ~ loop.index -%}
					{%- set answer_id = block.id ~ '-answer-' ~ loop.index -%}

					<div
						class="js-accordion-panel border-border border-b py-6"
						data-accordion-open="{{ loop.first ? 'true' : 'false' }}"
						data-accordion-deselect="true"
						itemscope
						itemprop="mainEntity"
						itemtype="https://schema.org/Question"
					>
						<button
							type="button"
							class="js-accordion-header group text-title-md flex w-full cursor-pointer items-center gap-6 text-left text-primary aria-expanded:text-accent"
							aria-expanded="{{ loop.first ? 'true' : 'false' }}"
							aria-controls="{{ answer_id }}"
							id="{{ question_id }}"
						>
							{%- if item.question -%}
								<span class="min-w-0 flex-1" itemprop="name">
									{{- item.question -}}
								</span>
							{%- endif -%}

							<span class="text-subtle shrink-0 group-aria-expanded:hidden" aria-hidden="true">+</span>
							<span class="hidden shrink-0 group-aria-expanded:inline" aria-hidden="true">−</span>
						</button>

						<div
							class="transition-[max-height] duration-300 ease-out"
							id="{{ answer_id }}"
							role="region"
							aria-labelledby="{{ question_id }}"
							itemscope
							itemprop="acceptedAnswer"
							itemtype="https://schema.org/Answer"
						>
							<div class="js-accordion-inner text-sm text-muted [&_a]:text-accent pt-4 [&_a]:underline" itemprop="text">
								{{- item.answer | raw -}}
							</div>
						</div>
					</div>
				{%- endfor -%}
			</cinq-accordion-group>
		{%- endif -%}
	</div>
</section>
