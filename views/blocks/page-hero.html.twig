{#
 # Blocks: Page Hero
 # Inner-page hero: overline, title, lead, then two CTAs.
 #}

{{-
	include(
		'blocks/_layout.html.twig',
		{
			paddings: block.layout.paddings,
			id: block.id
		}
	)
-}}

<section
	id="{{ block.id }}"
	{{ block.content.title ? 'aria-labelledby="' ~ block.id ~ '-title"' }}
>
	<div class="layout-grid container">
		<div class="col-span-full space-y-14 lg:col-span-9">
			{%- if block.content.overline or block.content.title or block.content.text -%}
				<div class="space-y-8">
					{%- if block.content.overline or block.content.title -%}
						<header class="space-y-6">
							{%- if block.content.overline -%}
								<p class="text-label text-taupe uppercase">
									{{- block.content.overline -}}
								</p>
							{%- endif -%}

							{%- if block.content.title -%}
								<h1 id="{{ block.id }}-title" class="text-title-xxl">
									{{- block.content.title -}}
								</h1>
							{%- endif -%}
						</header>
					{%- endif -%}

					{%- if block.content.text -%}
						<p class="text-taupe max-w-190 text-xl">
							{{- block.content.text | raw -}}
						</p>
					{%- endif -%}
				</div>
			{%- endif -%}

			{%- if block.content.links.0.title or block.content.links.1.title -%}
				<div class="flex flex-wrap items-center gap-4">
					{%- for link in block.content.links -%}
						{{-
							include(
								'components/button.html.twig',
								link
									| default({})
									| merge({
										variant: loop.first ? 'primary' : 'secondary'
									}),
								with_context = false
							)
						-}}
					{%- endfor -%}
				</div>
			{%- endif -%}
		</div>
	</div>
</section>
